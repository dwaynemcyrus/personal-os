 Everything currently built — keep or drop for v1?                                    
                                                                                  
  Core views (navigation)                                                              
  - Today / Now — daily dashboard showing date, tasks due today, quick actions       
  - Notes — notes list with groups (All, Pinned, Today, Todo, Trash) + note editor     
  - Tasks — task list with filters (inbox, active, someday, backlog, etc.) + task      
  detail sheet                                                                         
  - Plans — plans list + plan detail                                                   
  - Projects — project list + project detail sheet                                     
                                                                                       
  Data types in the database                                                           
  - note — notes with markdown content, tags, pinning, version history                 
  - task — tasks with status, priority, due dates, depends_on, scheduling              
  - capture — quick captures from the capture sheet
  - source — saved URLs for the reader section
  - plan — plans/goals
  - project — projects with areas
  - area — areas of responsibility
  - habit + habit_entry — habit tracking
  - template — note/task templates
  - objective + key_result — OKRs

  Sheets / overlays
  - Capture sheet — FAB → quick text capture
  - Context sheet — FAB long press → bottom sheet with Notes / Tasks / Plans / Sources
  tabs
  - Focus sheet — focus/timer mode
  - Inbox wizard — process captured items
  - Version history — per-note version restore

  Editor features
  - Rich markdown editor (Plate.js — being rebuilt)
  - Source mode toggle
  - Frontmatter / properties panel
  - Wikilinks [[note]]
  - Tags #tag
  - Slash commands
  - Task emoji cycling
  - Formatting toolbar + mobile accessory bar

  Infrastructure
  - Supabase auth (email/password) — keep
  - Offline sync (RxDB → PowerSync)
  - PWA (installable, home screen)
  - Timer (useTimer — time tracking against tasks/projects)
